# Gift House — Render

This package is based on the latest Gift House project supplied by the user.
It is configured as a Render Static Site.

Render settings:
- Build Command: `npm install && npm run build`
- Publish Directory: `dist`

Do not use Vercel/Manus build settings for this package.
